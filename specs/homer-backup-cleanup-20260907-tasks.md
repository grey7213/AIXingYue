# 2026-09-07 备份清理执行记录

- [x] 读取项目规则、原有备份 SPEC 和工具；核对当前 DNS/SSH 与磁盘。
- [x] 生成最新八类归档并下载到本地。
- [x] 校验全部哈希、归档内容和数据库完整性。
- [x] 服务器保留本次完整压缩备份并验证恢复材料。
- [x] 审查冗余备份精确路径清单并执行清理。
- [x] 复核磁盘、数据库、服务和公网健康，记录实际结果。
- [x] 更新项目导航、稳定运维规则和必要的备份工具修正；归档和一次性脚本留在仓库外/忽略目录。

## 实际结果

- 抓取时间：`2026-09-07T15:06:36+08:00`；完成本地八档下载与解压校验用时 1,165 秒。
- 本地目录：`E:\homer-backups\homer-prod-20260907-150635\`；另交付同名 `.zip`，ZIP 及成员哈希证据见 `output/backup-cleanup-20260907/delivery-verification.json`。
- 8 个归档共 `1,041,868,038 B`（993.60 MiB），解压内容共 `6,653,265,920 B`（6.20 GiB）。包含 DB、媒体、对话数据、runtime 源码、Web 前端、公开 APK/版本元数据、后端、系统配置。
- 数据库独立快照 `2,660,167,680 B`、56 张表，`quick_check=ok`、`integrity_check=ok`、`journal_mode=delete`；用户 124、角色 8,793、版本 8,804、会话 620、消息 2,468。
- 全部归档远端/本地 SHA-256 一致并能真正解压；独立工具 **35/35** 通过，含媒体 8,583 个文件、6 张抽样封面逐字节比对、对话归档 37,155 个条目和关键配置路径。
- 服务器完整压缩恢复点：`/opt/homer-backups/homer-prod-20260907-150635/`。新单文件 DB 已移至 `/opt/ai-fengyue-backend/backups/ai_fengyue-current-20260907-150635.sqlite3`，SHA-256 与本地解压 DB 相同；新前端包为同目录 `frontend-source-current-20260907-150635.tgz`，展开内容 SHA-256 与本地前端归档相同。
- 精确删除 265 个旧备份文件，实际占用 `6,246,670,336 B`（5.82 GiB）：旧后端源码 35、旧数据库/前端快照及辅助文件 8、旧对话部署快照 6、旧 APK 发布前 DB 1、较早的定向发布副本 215。删除前复验主机、全部保留包哈希和待删文件的路径/类型/inode/device/大小/mtime；未递归清理业务目录。
- 本次临时 server-config 明文副本已清理，暂存根目录已不存在。保留最近 2026-09-05 定向发布批次、所有 runtime releases、原始官方角色导入包及独有迁移/安全资料。
- 最终 `/`：总 `83,053,432,832 B`，已用 `28,890,980,352 B`，可用 `54,145,675,264 B`（**50.43 GiB**），使用率 **35%**。初始可用 `51,615,367,168 B`（38%）；新增完整恢复点和更新单文件 DB 后，净可用空间仍增加约 2.36 GiB。
- 清理后 live DB `quick_check=ok`，用户/角色/版本/会话/消息行数前后完全一致；backend/dialogue/Nginx 全为 active；Nginx 配置通过；内外 `/health=OK`，dialogue `/csrf-token=200`。全程未主动停止或重启服务。
- 本地新备份目录和 ZIP 仅授权当前用户、SYSTEM、Administrators；生产保留目录/文件为 `0700/0600`。业务库和对话数据也标为私密归档。
- 最后一轮检查修复了两个真实问题：GNU sha256sum 不接受 CRLF 文件名尾缀，以及 Cygwin scp 写入的显式宽泛 ACL。现服务器原生 `sha256sum -c` 8/8 通过，本地目录/17 个文件/ZIP 共 19 个路径的非白名单 ACL 为 0；备份工具已修正未来写入与下载权限。

## 证据与后续边界

- 本地备份目录内附 `MANIFEST.json`、`SHA256SUMS.txt`、`RESTORE.md`、`VERIFICATION.json`、`INDEPENDENT-VERIFICATION.log`、`CLEANUP-PLAN.json`、`CLEANUP-REPORT.json`、`SERVER-RETENTION.json`、`SUMMARY.md`。
- 删除清单 SHA-256：`ecab34eb3582448b7b335e0bee41cf4c3664b1a2bf7198e740881271129333fa`。一次性操作脚本和主机盘点写入忽略目录 `output/backup-cleanup-20260907/`，不提交其中的数据或完整命令日志。
- 2026-08-24 SPEC 的“不删旧备份”仅为那次操作边界；本次按用户明确要求，在完整验证后执行清理。
- 本次没有增加自动任务。将来再次清理仍需先拿到最新完整备份，不能直接重跑带本次固定路径的清理脚本；若要长期增量/周期备份，另行设计。

# 邮箱验证码可靠性任务

- [x] 建立独立验证码数据库与投递状态表。
- [x] 实现验证码复用和 60 秒冷却。
- [x] 注册、重置接口接入新库并兼容旧码。
- [x] 改善前端到达时间和拦截提示。
- [x] 本地锁库并发验证：主库 `BEGIN IMMEDIATE` 时独立库创建、复用、核销均通过。
- [x] 线上备份、部署和接口验证：服务与 Nginx active，内外 `/health` 均为 OK，接口返回结构化 accepted/retry_after。

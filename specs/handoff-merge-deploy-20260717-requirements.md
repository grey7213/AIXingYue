# 惑梦前后端交接合并需求（2026-07-17）

## 目标

- 将 `惑梦-Homer-前后端开发交接-20260717.zip` 中的制卡、卡片体验运行时、跨平台导入和登录态改动合并到当前工作区。
- 保留当前工作区尚未提交的 RP Hub 受限资源兼容、官推卡池、首页/后台样式与已有高级创作权限。
- 完成前后端、数据库迁移、浏览器安全边界、核心业务和生产部署回归。

## 范围

- 合并交接快照中相对当前工作区有差异的正式前端与后端源码。
- 新增惑梦数据合并工具页及工坊入口。
- 验证制卡素材、Galgame、BGM、弹窗、侧栏、实时数据、搜索筛选、插入输入框和 RP Hub 兼容。
- 验证官推池、角色导入、Cookie/Bearer 双认证、注册登录、聊天扣费、农场、支付元数据及管理员权限。
- 不覆盖生产 SQLite，不部署交接包中的本地数据库，不发布或重建 APK。

## 验收标准

- Python/JavaScript 语法、差异检查和临时 SQLite 初始化通过。
- 交接新增功能有定向测试，已有关键 API 回归无破坏。
- 桌面端和 390px 页面无横向溢出、阻断性控制台错误或沙箱越权。
- 卡片运行时仍不获得主站 DOM、token、localStorage 或任意网络访问。
- 部署前完成后端、前端、Nginx 配置和 SQLite 备份。
- 部署后 backend/Nginx active，内外 `/health` 为 `OK`，`CONTENT_MODE=local_only`，SQLite `quick_check=ok`。
- 线上创建的临时用户、角色、官推标记、素材、会话和订单全部清理。

## 非目标

- 不迁移历史技术命名、APK 包名、服务名或数据库名。
- 不恢复普通用户 BYOK，不暴露模型 API Key。
- 不扩大卡片外部资源白名单或移除既有 iframe 安全边界。

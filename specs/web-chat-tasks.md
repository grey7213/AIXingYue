# AI星月 Web 角色聊天任务

| ID | 任务 | 状态 | 验证 |
|----|------|------|------|
| WC1 | 补齐 Web 角色聊天需求和设计 SPEC | Done | `web-chat-requirements.md` / `web-chat-design.md` / 本文件 |
| WC2 | 后端新增站点级模型 API 配置表和管理员接口 | Done | `py_compile`；本地 `GET/POST /admin/api/llm-settings` 通过 |
| WC3 | 聊天调用优先读取数据库模型配置 | Done | 本地 Web chat 返回 `model_id=test-model` |
| WC4 | 新增角色详情页和前端详情 API 接入 | Done | 新增 `/app/character.html?id=...` 和 `character.js` |
| WC5 | 改造移动端 App 壳和底部导航 | Done | `/app/*.html` 主要页面已加入 `.app-bottom-nav` |
| WC6 | 创建页去掉普通用户 API Base 暴露，改为站点默认 API | Done | `create.html/js` 不再提交 `api_base_url` |
| WC7 | 后台页面接入模型 API 配置 | Done | `admin.html` + `admin-app.js`，key 仅脱敏展示 |
| WC8 | 更新 AI星月 current-state 和项目状态文档 | Done | 已记录 `CONTENT_MODE=local_only` 和新增 Web 能力 |
| WC9 | 部署到 `patcher.villainy.top` 并线上验证 | Done | health/explore/detail/chat/create/admin 配置均通过 |
| WC10 | 后台发布/管理官方角色卡 | Done | `admin/api/apps*`；后台“角色卡”页；线上创建/列表/详情/聊天/删除验证 |
| WC11 | 后台官方角色卡编辑字段和封面上传补强 | Done | 返回 `pre_prompt`/`llm_model`/`status`/`sort_weight`；后台上传封面按钮已接入 |
| WC12 | 接手后线上回归 APK/Web 共享后端与角色流程 | Done | 2026-06-18 真实请求验证 health、页面 200、注册/登录、用户角色创建/聊天/删除、后台模型配置脱敏、官方角色创建/详情/删除、探索总数 451 |
| WC13 | 新用户 5000 积分与单次聊天 50 积分扣费 | Done | 本地 `verify_chat_credits_local.py` 和线上 `verify_chat_credits_remote.py` 均验证注册 5000、阻塞聊天 4950、流式聊天 4900、余额不足 402 |
| WC14 | 充值/订阅档位与注册反薅 | Done | 线上 `deposit-meta` 返回 `1 CNY = 1000`、4 个积分包和 3 个订阅额度包；本地 DB 验证同邮箱/同 IP 发码与注册免费额度限制；浏览器截图验证前台/后台展示 |
| WC15 | Web 平台 Logo 替换 | Done | 用户提供黑白斜杠 Logo 已替换 Web 品牌图、默认头像、角色占位和空状态图；Playwright 验证图片加载和页面无错误 |
| WC16 | 爱发电购买页与卡密运营脚本 | Done | 线上 `aifadian_url=https://ifdian.net/a/villainy` 且 `payment_available=true`；新增 `tools/aifadian_redeem_ops.py` 和 `specs/aifadian-redeem-runbook.md` |
| WC17 | 关闭用户自定义 API 接入 | Done | 用户页移除模型连接器；创建页只加载站点模型；后端默认 `USER_BYOK_ENABLED=0`，拒绝保存用户模型并让旧 `user:` 引用回落到站点模型 |
| WC18 | 聊天页当前模型选择 | Done | `/app/chat.html` 增加“当前模型”下拉；只列出后台启用的站点模型；发送、重生成、生成新 swipe 均带 `model_id`；后端白名单校验公开模型 ID，不接受用户自定义 API |
| WC19 | 受限 Tavo/高级 HTML 渲染与长对话加载优化 | Done | `/app/chat.html` 检测 fenced/raw HTML 后只渲染到 `iframe sandbox="allow-scripts"`，不含 `allow-same-origin`；`srcdoc` 注入 CSP 禁止 connect/frame/object/form/external script；普通长文本折叠；消息接口默认最近 80 条并支持 `before` 加载更早 |
| WC20 | 聊天最终回复格式规范 | Done | 后端忽略上游 `reasoning_content`，并在模板/正则后执行最终可见回复清洗；前端 assistant 历史渲染同样清洗 JSON/英文过程标题。本地、线上 E2E 和浏览器验证均通过 |
| WC21 | Tavo 高级渲染设置与 TavoJS 兼容桥 | Done | `/app/chat.html` 新增高级渲染设置面板、JavaScript 模式、TavoJS 确认和公式显示；TavoJS 只在无 same-origin 的 sandbox iframe 内运行，CSP 继续禁止网络、子 frame、表单和外部脚本；本地和线上 Playwright 验证通过 |
| WC22 | HTML 代码注入器渲染兼容修复 | Done | 修复 DOMParser 将片段开头 `<style>/<script>` 搬到 `<head>` 导致脚本早于 `.mes_text` 目标执行的问题；支持带说明/未闭合 HTML code fence 和事件属性脚本模式；本地和线上 Playwright 验证 `injector_ran=true`、`unclosed_fence_ran=true`、`event_only_ran=true` |
| WC23 | Tavern 开场视觉页渲染和交互修复 | Done | 2026-07-03 目标卡 `admin-rczip-9721d5969c2effd819af` 新会话首条 assistant 先执行 Prompt Template 再执行 Regex，支持从 `extensions.regex_scripts` 读取完整大段替换；前端 sandbox 提供 ST top/storage 兼容代理。线上 Playwright 验证显示 `晚上好，欢迎回来`、5 个入口、章节弹窗打开，console/page error 为 0 |
| WC24 | Tavo `.thm` 深色主题与文字对比修复 | Done | 2026-07-03 解析 `Tavo_主题效果_14G5y(1).thm` 后确认其为深色主题；`chat.js` 的 sandbox `srcdoc` 注入对应 SmartTheme 深色变量、深色 iframe 背景和默认控件样式，`chat.html` cache-buster 更新为 `20260703-tavo-theme`。本地/线上 Tavo 高级渲染、沙箱安全和目标开场视觉页验证通过 |

## 本轮完成记录

- 2026-06-18 本地完成后端站点级模型 API 配置、角色详情页、移动端底部导航、创建页默认站点 API 和后台配置页。
- 本地验证命令：`D:\Anconda3\python.exe -m py_compile .\tools\ai_fengyue_local_server.py .\tools\deploy_ai_fengyue_villainy.py` 通过。
- 本地烟测：`/health` OK；管理员模型配置读写通过；创建角色、详情读取、`/console/api/web/chat` 均成功；API key 只返回 `has_api_key` 和尾号预览。
- 2026-06-18 已部署到 `https://patcher.villainy.top/`。线上验证：`ai-fengyue-backend.service` 与 `nginx` 均 active；`CONTENT_MODE=local_only`；`/app/login.html`、`/app/explore.html`、`/app/character.html`、`/app/chat.html`、`/app/create.html`、`/app/my-apps.html`、`/app/me.html`、`/admin.html` 均 HTTP 200。
- 线上接口验证：无 token 访问 `/admin/api/llm-settings` 返回 `failure/status=403`；管理员 token 访问成功；探索返回本地角色总数 167；角色详情可读；创建 Web 角色成功；`/console/api/web/chat` 返回成功回复。
- 移动端浏览器验证：生成 `output/playwright/login-mobile.png`、`explore-mobile.png`、`character-mobile.png`、`create-mobile.png`；底部导航 5 项，创建页无 API Base 输入框。
- 2026-06-18 继续补齐后台官方角色卡能力：管理员可在 `/admin.html` 的“角色卡”页创建、查看、编辑、删除 `source=admin` 官方角色；官方角色出现在探索页并使用站点模型 API 聊天。
- 线上官方角色卡验证：`POST /admin/api/apps/create` 返回 `source=admin`；`GET /admin/api/apps?source=admin` 可列出草稿/私有角色；`/console/api/apps/{id}` 可读详情；`/console/api/web/chat` 返回成功回复；测试角色删除成功。
- 2026-06-18 补强后台角色卡编辑字段：`local_app_to_card()` 现在返回 `pre_prompt`、`llm_model`、`status`、`is_public`、`sort_weight`；后台角色弹窗支持封面上传。线上字段验证角色创建后返回 `field-test-model`、`draft`、`is_public=false`、`sort_weight=77`，列表可见，测试角色已删除。
- 2026-06-18 接手后回归：`https://patcher.villainy.top/health` 返回 `OK`；`ai-fengyue-backend.service` 与 `nginx` 均 `active`；`CONTENT_MODE=local_only`；`/`、`/admin.html`、`/app/login.html`、`/app/explore.html`、`/app/character.html`、`/app/chat.html`、`/app/create.html`、`/app/my-apps.html`、`/app/me.html` 均 HTTP 200。
- 2026-06-18 一次性普通账号 `codexchat...@test.com` 验证：发送验证码、注册、登录、创建 `source=user` 角色、用 `/console/api/web/chat` 的 `content` 字段聊天、读取会话 2 条消息、删除角色均成功。
- 2026-06-18 管理员 API 验证：无 token 访问 `/admin/api/llm-settings` 返回 `403`；使用 `ADMIN_EMAILS=local@ctf.test` 对应本地 token 访问 `/admin/api/whoami` 和 `/admin/api/llm-settings` 成功，模型配置不回显明文 `api_key`；`/admin/api/apps?source=upstream&page=1&page_size=1` 返回总数 `449`；临时官方角色创建、详情读取、删除均成功。
- 注意：不要再假设 `local@ctf.test` 密码是 `local123456`。当前线上该邮箱仍是管理员邮箱，但密码已变化；需要后台接口验收时可在服务器 DB 中读取该用户 id 并按 `local.` token 结构生成临时 bearer token，不要改密码。
- 2026-06-27 新用户注册默认额度改为 5000 免费积分；每次成功生成角色回复消耗 50 积分。扣费覆盖 Web 单聊流式/阻塞、旧 chat-messages 兼容入口、群聊角色回复、重新生成和生成新 swipe；只切换已有 swipe 不扣费。扣费顺序为 free -> reward -> paid，生成失败不扣费。
- 2026-06-27 本地验证：`D:\Anconda3\python.exe .\output\verify_chat_credits_local.py` 返回 `registered_points=5000`、`after_blocking_chat=4950`、`after_stream_chat=4900`、`insufficient_code=402`。
- 2026-06-27 线上验证：部署后 `python3 /tmp/verify_chat_credits_remote.py` 返回 `remote_initial_points=5000`、`after_blocking_chat=4950`、`after_stream_chat=4900`、`insufficient_code=402`、`site_copy_5000=true`；临时用户和临时角色清理检查均为 0。
- 2026-06-27 本轮新增目标：把旧 `1 CNY ≈ 10000` 积分充值口径改为 `1 CNY = 1000` 积分；推荐积分包为 `10/20/50/100` 元，对应 `10000/22000/57500/120000` 积分；订阅仅做月度额度包展示，不做无限用；新增注册发码和注册成功免费额度领取的基础 IP 限制；Web 默认 Logo/空态/默认头像改为用户提供的黑白斜杠 Logo。
- 2026-06-27 已完成 WC14/WC15：后端默认充值口径改为 `1 CNY = 1000 星月币，50 星月币约等于 1 次角色回复`；积分包为 `10 -> 10000`、`20 -> 22000`、`50 -> 57500`、`100 -> 120000`；订阅展示为月度额度包 `19.9 -> 22000`、`39.9 -> 50000`、`79.9 -> 110000`，不承诺无限用。
- 2026-06-27 反薅实现：重复邮箱不能再次发码/注册；同邮箱每小时最多 3 次发码，同 IP 每小时最多 8 次发码；同 IP 每天最多 3 个新注册账号可领取免费额度；新增 `user_security_events`、`register_ip`、`last_login_ip` 记录。
- 2026-06-27 部署与验证：`D:\Anconda3\python.exe -m py_compile .\tools\ai_fengyue_local_server.py` 通过；`node --check` 覆盖 `layout.js`、`hub-pages.js`、`me.js`、`admin-app.js`；线上 `/health` OK，`ai-fengyue-backend.service` 与 `nginx` active，`CONTENT_MODE=local_only`；`/console/api/web/deposit-meta` 返回新套餐/订阅。
- 2026-06-27 浏览器验收：`D:\Anconda3\python.exe .\output\verify_billing_logo_browser.py` 返回 `app_logo_loaded=true`、`default_avatar_loaded=true`、`empty_logo_loaded=true`、`avatar_placeholder_loaded=true`、`rewards_rate_ok=true`、`packages_ok=true`、`subscriptions_ok=true`、`mobile_subscriptions_ok=true`、`admin_subscription_editor_ok=true`、`console_error_count=0`、`page_error_count=0`。截图保存在 `output\playwright\billing-*.png`。
- 2026-06-27 已配置爱发电购买页 `https://ifdian.net/a/villainy`。`D:\Anconda3\python.exe .\tools\aifadian_redeem_ops.py configure --purchase-url https://ifdian.net/a/villainy` 返回 `configured=true`、`packages=4`、`subscriptions=3`；公开 `/console/api/web/deposit-meta` 返回 `payment_available=true`。
- 2026-06-27 新增爱发电卡密运营脚本 `tools\aifadian_redeem_ops.py`：`configure` 可写入购买链接和套餐，`generate --plan <id|all> --count N` 可调用后台兑换码 API 生成库存并导出 CSV/TXT。新增 `specs\aifadian-redeem-runbook.md` 记录爱发电商品档位、发货文案、卡密流程和后续自动绑定账号方案。
- 2026-06-28 已生成首批爱发电自动发货库存：`output\aifadian-codes\20260628-001621\`，7 个固定档位每档 20 个码。每个档位目录包含 `管理库存.csv`、`卡密库存-一行一码.txt`、`自动发货完整文案.txt`、`商品说明.md`；总配置清单为 `爱发电商品配置总表.md`。远程 DB 校验 7 个爱发电备注分组均为 `20` 个未使用码。
- 2026-06-28 爱发电后台实际创建/编辑商品被登录态阻塞：agent-browser 打开 `https://ifdian.net/a/villainy` 和登录页只能看到“注册/登录”与邮箱/密码输入框，无法访问截图中的已登录“管理/设置”入口。需要用户在 agent-browser 打开的可见窗口中登录后继续导入库存。
- 2026-06-29 已按用户要求关闭普通用户自定义 API 接入：`frontend/app/me.html` 不再显示模型连接器；`frontend/app/assets/js/me.js` 不再请求或保存用户模型；`frontend/app/assets/js/create.js` 只加载 `/console/api/web/model-presets` 的站点模型，编辑旧 `llm_model=user:*` 角色时自动切回站点默认；后端默认 `USER_BYOK_ENABLED=0`，`/console/api/web/user-model-presets` GET 返回空/disabled，POST 返回 403，聊天解析旧 `user:*` 时回落站点模型。
- 2026-06-29 聊天页新增当前模型选择：`frontend/app/chat.html` 在输入区加入“当前模型”下拉，`frontend/app/assets/js/chat.js` 加载 `/console/api/web/model-presets` 并按角色本地记住选择；`sendChatStream`、`regenerate`、`swipe next` 传递 `model_id`；后端 `public_model_selection()` 只允许公开站点模型 ID，选择后会覆盖当前请求的 `llm_model`，不修改角色卡，不恢复普通用户自定义 API。
- 2026-06-29 “我的”页移动端顶部标题清理：`frontend/app/me.html` 的浏览器标题从 `我的 · AI星月` 改为 `我的`，并更新 `me.js` cache-buster。线上 HTML 检查确认旧标题和“我的模型连接器/自带 Key”文案不存在；`output/verify_me_title_clean_browser.py` 独立 Playwright 手机尺寸验收返回 `document_title=我的`、`topbar_title=我的`、旧连接器文案和 API Key 输入不存在、`console_error_count=0`、`page_error_count=0`，截图为 `output/playwright/me-title-clean-mobile.png`。
- 2026-06-29 导入用户提供的 `角色卡.zip`：压缩包密码为用户提供的 `123`。解密后 277 个候选文件中，95 个通过角色卡字段校验并作为 `source=admin` 官方公开角色导入，批次号 `role-card-zip-20260629`；90 张 PNG 封面被压缩后上传到 `/media-cache/cover/`；10 个命中未成年人/校园等高风险关键词的候选被跳过，82 个为图片生成 metadata/工作流/Regex 脚本等非角色卡，78 个 PNG 无可识别角色卡 metadata。线上验证：`local_apps` 总数 `543`，本批 `admin=95/public=95`；抽样 `Blacksouls` 详情可读、世界书 43 条、封面 HTTP 200；临时用户聊天成功，回复有内容且积分从 5000 扣到 4950，测试用户已清理。
- 2026-06-29 已配置 CelestiAI 站点级 OpenAI-compatible 模型预设：`celestiai-gemini-25-pro`，Base URL 为 `https://api.celestiai.xyz/v1`，默认模型 `gemini-2.5-pro-cli`，公开模型列表包含 6 个 CelestiAI 模型且不返回 API Key。CelestiAI 会对默认 Python/urllib User-Agent 返回 Cloudflare 1010，后端已给上游 LLM 请求补充浏览器式 `User-Agent`/`Origin`/`Referer`。线上验证：直连 CelestiAI 成功；临时用户对导入角色 `Blacksouls` 走 `/console/api/web/chat/stream` 返回 9 个 delta、`message_end=true`，回复 3344 字符，积分 `5000 -> 4950`，测试用户清理为 0；`ai-fengyue-backend.service`、`nginx` active，`CONTENT_MODE=local_only`，公开 `/health` OK。
- 2026-06-29 按用户要求取消内容题材筛选，重新导入 `角色卡.zip` 中所有“可直接使用”的角色卡，并把 SillyTavern `extensions.regex_scripts` / `TavernHelper_scripts` 转成平台顶层 `regex_scripts`。本次解析 277 个候选文件，199 个有角色卡 metadata，118 个有可用设定/开场/世界书/问候，95 个已导入卡被更新，23 个之前未导入的可用卡被新增；75 个解析后为空壳或仅脚本/工作流配置，78 个 PNG 无角色 metadata，未作为角色卡导入。线上结果：`local_apps_total=566`、`admin_total=118`、新增 23 张均公开发布，59 张带非空顶层 regex。样本新卡 `admin-rczip-all-6545f4abddf781438e0c` 详情可读，世界书 13 条、regex 10 条、封面 HTTP 200；真实流式聊天返回 69 个 delta、`message_end=true`、回复 823 字符，积分 `5000 -> 4950`，测试用户清理为 0；服务/Nginx active，`CONTENT_MODE=local_only`，日志无 regex/LLM 异常。
- 2026-06-29 实现 WC19：`frontend/app/assets/js/chat.js` 新增 Tavo/高级 HTML 检测、`srcdoc` 构造、iframe resize、主 DOM 安全转义和长文本折叠；`frontend/app/chat.html` 增加可视化 iframe 样式、宽消息行和“加载更早消息”；`frontend/app/assets/js/app-core.js` 支持 messages query 参数；`tools/ai_fengyue_local_server.py` 的 `list_messages()` 改为取最新消息并支持 `before_created_at`，新增 `count_messages()`，Web 消息接口返回 `total/has_more/next_before`。
- 2026-06-29 本地验证：`node --check frontend/app/assets/js/chat.js frontend/app/assets/js/app-core.js` 通过；`D:\Anconda3\python.exe -m py_compile tools\ai_fengyue_local_server.py output\verify_tavo_sandbox_browser.py output\verify_message_pagination_local.py` 通过；`output\verify_tavo_sandbox_browser.py` 返回 `hasIframe=true`、`sandbox=allow-scripts`、`sandboxHasSameOrigin=false`、`mainScriptCount=0`、`srcdocHasCsp=true`、`scriptPostMessageReceived=true`、`hasLongTextCollapse=true`、`console_error_count=0`、`page_error_count=0`，截图 `output\playwright\chat-tavo-sandbox-local.png`；`output\verify_message_pagination_local.py` 证明 120 条消息默认取最新并可向前翻页。
- 2026-06-29 线上部署与验证：`D:\Anconda3\python.exe .\tools\deploy_ai_fengyue_villainy.py --skip-apk --skip-mail-install --skip-certbot` 成功；`ai-fengyue-backend.service` 和 `nginx` 均 `active`，`/health` 返回 `OK`，`CONTENT_MODE=local_only`。线上 `/app/chat.html` 已引用 `chat.js?v=20260629-tavo-sandbox`，线上 `chat.js` 包含 `sandbox="allow-scripts"`、`connect-src 'none'`、无 `allow-same-origin`、长文本折叠和 `loadOlderMessages`。`VERIFY_BASE_URL=https://patcher.villainy.top D:\Anconda3\python.exe .\output\verify_tavo_sandbox_browser.py` 返回同样的 sandbox/CSP/折叠检查并生成 `output\playwright\chat-tavo-sandbox-live.png`。远程 `/tmp/verify_message_pagination_remote.py` 验证线上接口 `latest_total=120`、最新窗口 `message-116..120`、更早窗口 `message-111..115`、顺序升序；临时用户/会话/消息清理后均为 0。
- 2026-07-02 完成 WC20：`tools/ai_fengyue_local_server.py` 的系统提示新增禁止推理/JSON/英文过程标题规则；`extract_stream_delta()` 不再把 `reasoning_content` 当正文；`process_model_reply()` 在 Prompt Template、世界书 render 注入和 Regex 后调用 `normalize_visible_chat_reply()`。`frontend/app/assets/js/chat.js` 对 assistant 历史消息使用同一展示级清洗，`frontend/app/chat.html` cache-buster 更新为 `20260702-reply-format`。
- 2026-07-02 验证：`D:\Anconda3\python.exe -m py_compile .\tools\ai_fengyue_local_server.py .\output\verify_reply_format_local.py .\output\verify_reply_format_remote.py .\output\verify_reply_format_browser.py` 通过；`node --check .\frontend\app\assets\js\chat.js` 通过；本地 `verify_reply_format_local.py` 覆盖英文过程标题、JSON/fenced JSON、Regex 后清洗、状态块/HTML 保留和 `reasoning_content` 丢弃；线上假上游 E2E 返回并保存 `你好 星月`，无 JSON/Processing/reasoning/SECRET，积分 `5000 -> 4950`，临时用户/角色/消息清理为 0；浏览器验证 `bubble_text=你好，星月。`、console/page error 为 0，截图 `output/playwright/chat-reply-format-live.png`。
- 2026-07-02 完成 WC21：`frontend/app/chat.html` 工具栏新增“高级渲染”设置面板；`frontend/app/assets/js/chat.js` 增加本地持久化设置、TavoJS 确认门、`disabled/auto/script/code-block` 四种 JS 模式、iframe 内 `window.TavoJS/window.tavo` 安全兼容桥，以及 LaTeX/AsciiMath 轻量公式显示。卡内脚本仍只在 `sandbox` iframe 内执行，不加 `allow-same-origin`，不允许网络、子 frame、表单或外部脚本。
- 2026-07-02 验证：`node --check .\frontend\app\assets\js\chat.js`、`D:\Anconda3\python.exe -m py_compile .\output\verify_tavo_advanced_render_browser.py .\output\verify_tavo_sandbox_browser.py` 通过；本地和线上 `verify_tavo_advanced_render_browser.py` 均返回 `default_confirm_paused=true`、`settings_persisted=true`、`tavo_bridge_ok=true`、`sandbox_has_same_origin=false`、`parent_can_read_frame=false`、`raw_script_ran=false`、`fenced_script_ran=true`、`formula_inline=true`、console/page error 为 0；线上旧沙箱回归 `verify_tavo_sandbox_browser.py` 也通过。截图：`output/playwright/chat-tavo-advanced-live.png`。
- 2026-07-02 完成 WC22：`frontend/app/assets/js/chat.js` 对 HTML 注入器片段做兼容修复。非完整 HTML 片段中被 DOMParser 放进 `head` 的 `<style>/<script>` 会移回 iframe body 兼容容器内执行；iframe 内新增 `#chat`、`.mes`、`.mes_text`、`.message-content` 等 SillyTavern 常见目标；HTML fence 支持 ` ```html injector ` 这类带说明语言和结尾未闭合的最后一个 code block；`script` 模式允许事件属性和 `javascript:` 这类可执行片段，但仍仅在 sandbox iframe 中运行。
- 2026-07-02 WC22 验证：`node --check .\frontend\app\assets\js\chat.js` 通过；`D:\Anconda3\python.exe .\output\verify_tavo_advanced_render_browser.py` 本地返回 `injector_ran=true`、`injector_text_has_result=true`、`unclosed_fence_ran=true`、`event_only_ran=true`；部署后 live 验证同样通过，且 `sandbox_has_same_origin=false`、`parent_can_read_frame=false`、`main_script_count=0`。线上 `chat.html` 已引用 `chat.js?v=20260702-html-injector`；`ai-fengyue-backend.service`、`nginx` active，公开 `/health` OK，`CONTENT_MODE=local_only`。
- 2026-07-03 完成 WC23：修复目标卡 `admin-rczip-9721d5969c2effd819af` 的开场视觉页只显示源码/首屏无交互问题。后端 `chat_greetings_from_card()` 对开场白执行 Prompt Template 后再执行角色 Regex；`enabled_regex_scripts()` 同时读取顶层 `regex_scripts`、`extra_settings.regex_scripts`、顶层 `extensions.regex_scripts` 和 `extra_settings.extensions.regex_scripts`，并优先使用同名同规则的更长替换内容；Regex 替换上限提升到 `240000` 字符，替换文本按 SillyTavern/JS `$1`、`$&`、`$<name>` 语义展开，避免 Python `re.sub` 误处理反斜杠和 `$`。
- 2026-07-03 WC23 前端：`frontend/app/assets/js/chat.js` 的 iframe sandbox 增加 `window.__xySTTop`、`window.SillyTavern.getContext()`、世界书空实现、iframe 内本地 `localStorage/sessionStorage` shim，并重写卡片脚本里的 `window.top`、`parent.SillyTavern`、storage 引用；内联 `type=module` 脚本转为普通沙箱脚本，内联事件属性也做同样重写。安全边界保持 `sandbox="allow-scripts"`，不加 `allow-same-origin`，主聊天 DOM 不插入卡内 `<script>`。
- 2026-07-03 WC23 验证：`py_compile`、`node --check`、`verify_tavern_opening_render_local.py`、`verify_tavern_template_local.py`、`verify_reply_format_local.py`、本地 `verify_tavern_opening_render_browser.py` 均通过。部署后远程 API 验证返回 `first_is_code_fence=true`、`first_has_hero=true`、`first_has_script=true`、`swipe_count=13`、`has_p6/h8/u6=true` 且临时数据清理为 0；线上浏览器验证 `hero_text=晚上好，欢迎回来`、labels 包含 `开关选项/游玩说明/小学篇/高中篇/大学篇`、`modal_opened=true`、`sandbox_has_same_origin=false`、`parent_can_read_frame=false`、console/page error 为 0。线上 `verify_tavo_advanced_render_browser.py` 与 `verify_tavo_sandbox_browser.py` 回归通过，服务/Nginx active，公开 `/health` OK，`CONTENT_MODE=local_only`。
- 2026-07-03 完成 WC24：`Tavo_主题效果_14G5y(1).thm` 是 `tavo_theme_v1` zip，`theme.json` 使用 `#222222` 背景、白色文字、半透明深色气泡。此前高级渲染 iframe 默认透明背景且没有 SmartTheme 变量，卡片脚本/样式可能回退到浅色外层 UI，导致部分白字或浅色字不可见。`frontend/app/assets/js/chat.js` 现在在 sandbox `srcdoc` 的 `:root` 和 `<html style>` 中注入 `--SmartTheme*` 深色变量，并给 iframe body、常用消息容器和默认控件设置深色可读兜底；`frontend/app/chat.html` 引用 `chat.js?v=20260703-tavo-theme`。
- 2026-07-03 WC24 验证：`node --check .\frontend\app\assets\js\chat.js` 和 `git diff --check` 通过；本地 `verify_tavo_advanced_render_browser.py`、`verify_tavo_sandbox_browser.py`、`verify_tavern_opening_render_browser.py` 均通过并截图确认深色卡面文字可读。部署后线上 health OK，`ai-fengyue-backend.service` 与 `nginx` active，`CONTENT_MODE=local_only`；线上 `chat.html` 已引用 `20260703-tavo-theme` 且 `chat.js` 包含 `--SmartThemeChatTintColor:#222222`；线上 Tavo 高级渲染/沙箱回归通过，目标页 `verify_tavern_opening_render_live.py` 返回 labels 齐全、弹窗可打开、console/page error 为 0。

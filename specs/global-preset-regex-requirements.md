# 全局提示词与正则预设需求

## 目标

- 后台维护站点级提示词预设库和正则预设库。
- 导入 `Tavo_梦鲸思客V4-0709 Copy_18bEj(3).json` 的全部 85 个提示词定义，不丢弃停用项、marker、空内容项、顺序信息或扩展字段。
- 导入 `Tavo_梦鲸思客V4-0709's Regex_18bDS(3).zip` 的全部 26 条正则，保留 ZIP 文件顺序和全部字段。
- 同一类型任意时刻只启用一个全局预设；启用新预设时原子停用同类旧预设，旧记录不删除。
- 全局提示词和正则应用到所有角色，不改写角色卡数据，也不影响用户已有对话记录。

## 验收标准

- 后台列表能看到旧预设和新预设，旧预设处于停用/搁置状态。
- 新提示词预设显示 85 条定义，其中 81 条有顺序配置、4 条仅保留定义。
- 新正则预设显示 26 条脚本，其中 24 条启用、2 条停用。
- 第一条、marker、停用条目、未排序条目和最后一条都能展开、编辑、保存，刷新后字段和值保持一致。
- 保存模型供应商配置不会覆盖或改变全局预设库。
- 运行时只使用当前启用的一个提示词预设和一个正则预设。
- 全局正则按原始顺序执行，并区分用户/AI placement、promptOnly、markdownOnly 和深度限制。
- 后端编译、前端语法检查、函数级验证、真实后台桌面/移动端浏览器验证和线上健康检查通过。

## 非目标

- 不修改 APK。
- 不删除旧预设。
- 不执行 ZIP 中的文件；仅解析 JSON 配置。
- 不将正则生成的脚本直接注入主页面 DOM。

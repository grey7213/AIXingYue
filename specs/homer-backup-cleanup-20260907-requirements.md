# 2026-09-07 生产备份与冗余清理

## 目标与范围

- 用户要求把 `patcher.villainy.top` 最新服务器数据完整备份到本地，然后删除多余服务器备份、释放磁盘。
- 当前 DNS 与 SSH 实测均为 `38.76.218.46`，主机名 `vm-851a1bc4a978a80f`；备份目录沿用仓库外 `E:\homer-backups\`。
- 覆盖业务 SQLite、media-cache、SillyTavern 用户数据、当前 runtime 源码、前端、公开 APK/版本元数据、后端与系统配置。
- 用户本次明确授权清理冗余服务器备份，覆盖 2026-08-24 SPEC 的“不清理旧备份”任务边界。

## 验收

1. SQLite Online Backup 快照可独立恢复，远端与本地哈希一致，本地 `quick_check`、`integrity_check` 通过。
2. 八类完整归档逐一校验哈希并真实解压，附清单、校验和、恢复说明。
3. 完成本地验证后，服务器保留一份经哈希校验的本次完整压缩备份；旧备份先列精确路径与理由，再按清单清理。
4. 清理不触及线上业务文件、当前 runtime、其他服务数据或独有的安全恢复材料；不删除本地既有备份。
5. 记录清理前后磁盘空间，目标可用空间至少 20 GiB；backend/dialogue/Nginx 及内外健康检查通过。

## 非目标

- 不部署功能、不更新 APK、不改定时任务、不升级依赖、不清理其他站点的数据。
- 不上传第三方存储或飞书，含密钥归档不得进入 Git。
